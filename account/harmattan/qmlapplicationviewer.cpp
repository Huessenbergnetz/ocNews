// checksum 0x78c version 0x60010
/*
  This file was generated by the Qt Quick Application wizard of Qt Creator.
  QmlApplicationViewer is a convenience class containing mobile device specific
  code such as screen orientation handling. Also QML paths and debugging are
  handled here.
  It is recommended not to modify this file, since newer versions of Qt Creator
  may offer an updated version of it.
*/

#include "qmlapplicationviewer.h"

#include <QtCore/QDir>
#include <QtCore/QFile>
#include <QtCore/QFileInfo>
#include <QtDeclarative/QDeclarativeComponent>
#include <QtDeclarative/QDeclarativeEngine>
#include <QtDeclarative/QDeclarativeContext>
#include <QtGui/QApplication>

#include <QDebug>

#include <QX11Info>
#include <X11/Xlib.h>
#include <X11/Xatom.h>

#include <qplatformdefs.h> // MEEGO_EDITION_HARMATTAN

#ifdef HARMATTAN_BOOSTER
#include <MDeclarativeCache>
#endif

#if defined(QMLJSDEBUGGER) && QT_VERSION < 0x040800

#include <qt_private/qdeclarativedebughelper_p.h>

#if !defined(NO_JSDEBUGGER)
#include <jsdebuggeragent.h>
#endif
#if !defined(NO_QMLOBSERVER)
#include <qdeclarativeviewobserver.h>
#endif

// Enable debugging before any QDeclarativeEngine is created
struct QmlJsDebuggingEnabler
{
    QmlJsDebuggingEnabler()
    {
        QDeclarativeDebugHelper::enableDebugging();
    }
};

// Execute code in constructor before first QDeclarativeEngine is instantiated
static QmlJsDebuggingEnabler enableDebuggingHelper;

#endif // QMLJSDEBUGGER

class QmlApplicationViewerPrivate
{
    QmlApplicationViewerPrivate(QDeclarativeView *view_) : view(view_) {}

    QString mainQmlFile;
    QDeclarativeView *view;
    friend class QmlApplicationViewer;
    QString adjustPath(const QString &path);
};

QString QmlApplicationViewerPrivate::adjustPath(const QString &path)
{
#ifdef Q_OS_UNIX
#ifdef Q_OS_MAC
    if (!QDir::isAbsolutePath(path))
        return QCoreApplication::applicationDirPath()
                + QLatin1String("/../Resources/") + path;
#else
    QString pathInInstallDir;
    const QString applicationDirPath = QCoreApplication::applicationDirPath();
    pathInInstallDir = QString::fromAscii("%1/../%2").arg(applicationDirPath, path);

    if (QFileInfo(pathInInstallDir).exists())
        return pathInInstallDir;
#endif
#endif
    return path;
}

QmlApplicationViewer::QmlApplicationViewer(QWidget *parent)
    : QDeclarativeView(parent)
    , d(new QmlApplicationViewerPrivate(this))
{
    connect(engine(), SIGNAL(quit()), SLOT(close()));
    setResizeMode(QDeclarativeView::SizeRootObjectToView);
    // Qt versions prior to 4.8.0 don't have QML/JS debugging services built in
#if defined(QMLJSDEBUGGER) && QT_VERSION < 0x040800
#if !defined(NO_JSDEBUGGER)
    new QmlJSDebugger::JSDebuggerAgent(d->view->engine());
#endif
#if !defined(NO_QMLOBSERVER)
    new QmlJSDebugger::QDeclarativeViewObserver(d->view, d->view);
#endif
#endif
}

QmlApplicationViewer::QmlApplicationViewer(QDeclarativeView *view, QWidget *parent)
    : QDeclarativeView(parent)
    , d(new QmlApplicationViewerPrivate(view))
{
    connect(view->engine(), SIGNAL(quit()), view, SLOT(close()));
    view->setResizeMode(QDeclarativeView::SizeRootObjectToView);
    // Qt versions prior to 4.8.0 don't have QML/JS debugging services built in
#if defined(QMLJSDEBUGGER) && QT_VERSION < 0x040800
#if !defined(NO_JSDEBUGGER)
    new QmlJSDebugger::JSDebuggerAgent(d->view->engine());
#endif
#if !defined(NO_QMLOBSERVER)
    new QmlJSDebugger::QDeclarativeViewObserver(d->view, d->view);
#endif
#endif
}

QmlApplicationViewer::~QmlApplicationViewer()
{
    delete d;
}

QmlApplicationViewer *QmlApplicationViewer::create()
{
#ifdef HARMATTAN_BOOSTER
    return new QmlApplicationViewer(MDeclarativeCache::qDeclarativeView(), 0);
#else
    return new QmlApplicationViewer();
#endif
}

void QmlApplicationViewer::setMainQmlFile(const QString &file)
{
    Atom atomMInvokedBy = XInternAtom(QX11Info::display(), "_MEEGOTOUCH_WM_INVOKED_BY", False);
    Display *display = QX11Info::display();
    XChangeProperty(display, d->view->winId(), atomMInvokedBy, XA_WINDOW, 32, PropModeReplace, (unsigned char *)&parentWindowId, 1);
    XSetTransientForHint(display, d->view->winId(), parentWindowId);
    Atom atomWindowType = XInternAtom(QX11Info::display(), "_MEEGOTOUCH_NET_WM_WINDOW_TYPE_MAPPLICATION", False);
    XChangeProperty(QX11Info::display(), d->view->winId(),
                    XInternAtom(QX11Info::display(), "_NET_WM_WINDOW_TYPE", False),
                    XA_ATOM, 32, PropModeAppend, (unsigned char*) &atomWindowType, 1);

    d->view->rootContext()->setContextProperty("actor", this);

    d->mainQmlFile = d->adjustPath(file);
    d->view->setSource(QUrl::fromLocalFile(d->mainQmlFile));
    QObject* themeObject = qvariant_cast<QObject*>(d->view->rootContext()->contextProperty("theme"));
    if ( themeObject )
       themeObject->setProperty("inverted", true);

    QObject* screenObject = qvariant_cast<QObject*>(d->view->rootContext()->contextProperty("screen"));
    if ( screenObject )
       screenObject->setProperty("allowedOrientations", 1);

}

void QmlApplicationViewer::addImportPath(const QString &path)
{
    d->view->engine()->addImportPath(d->adjustPath(path));
}

void QmlApplicationViewer::setOrientation(ScreenOrientation orientation)
{
#if defined(Q_OS_SYMBIAN)
    // If the version of Qt on the device is < 4.7.2, that attribute won't work
    if (orientation != ScreenOrientationAuto) {
        const QStringList v = QString::fromAscii(qVersion()).split(QLatin1Char('.'));
        if (v.count() == 3 && (v.at(0).toInt() << 16 | v.at(1).toInt() << 8 | v.at(2).toInt()) < 0x040702) {
            qWarning("Screen orientation locking only supported with Qt 4.7.2 and above");
            return;
        }
    }
#endif // Q_OS_SYMBIAN

    Qt::WidgetAttribute attribute;
    switch (orientation) {
#if QT_VERSION < 0x040702
    // Qt < 4.7.2 does not yet have the Qt::WA_*Orientation attributes
    case ScreenOrientationLockPortrait:
        attribute = static_cast<Qt::WidgetAttribute>(128);
        break;
    case ScreenOrientationLockLandscape:
        attribute = static_cast<Qt::WidgetAttribute>(129);
        break;
    default:
    case ScreenOrientationAuto:
        attribute = static_cast<Qt::WidgetAttribute>(130);
        break;
#else // QT_VERSION < 0x040702
    case ScreenOrientationLockPortrait:
        attribute = Qt::WA_LockPortraitOrientation;
        break;
    case ScreenOrientationLockLandscape:
        attribute = Qt::WA_LockLandscapeOrientation;
        break;
    default:
    case ScreenOrientationAuto:
        attribute = Qt::WA_AutoOrientation;
        break;
#endif // QT_VERSION < 0x040702
    };
    setAttribute(attribute, true);
}

void QmlApplicationViewer::showExpanded()
{

#if defined(Q_OS_SYMBIAN) || defined(MEEGO_EDITION_HARMATTAN) || defined(Q_WS_SIMULATOR)
    d->view->showFullScreen();
#elif defined(Q_WS_MAEMO_5)
    d->view->showMaximized();
#else
    d->view->show();
#endif
}

QApplication *createApplication(int &argc, char **argv)
{
#ifdef HARMATTAN_BOOSTER
    return MDeclarativeCache::qApplication(argc, argv);
#else
    return new QApplication(argc, argv);
#endif
}

void QmlApplicationViewer::init(int type)
{
    de::buschmann23::ocNewsEngine::Configuration* configIf = new de::buschmann23::ocNewsEngine::Configuration("de.buschmann23.ocNewsEngine", "/Configuration", QDBusConnection::sessionBus(), this);

    bool existing = configIf->isConfigSet();
    if (existing && type == 0)
    {
        initType = 2;
    } else {
        initType = type;
    }
    d->view->rootContext()->setContextProperty("initType", initType);
    d->view->rootContext()->setContextProperty("onloadbusy", QVariant(false));
    d->view->rootContext()->setContextProperty("usesslvalue", QVariant(true));

    manager = new Accounts::Manager();
}

void QmlApplicationViewer::onLoad()
{
    d->view->rootContext()->setContextProperty("enabledvalue", QVariant(accountx->enabled()));
    d->view->rootContext()->setContextProperty("usernametext", QVariant(accountx->valueAsString("username")));
    d->view->rootContext()->setContextProperty("passwordtext", QVariant(accountx->valueAsString("password")));
    d->view->rootContext()->setContextProperty("servertext", QVariant(accountx->valueAsString("server")));
    d->view->rootContext()->setContextProperty("usesslvalue", QVariant(accountx->valueAsBool("usessl")));
    d->view->rootContext()->setContextProperty("ignoresslerrorsvalue", QVariant(accountx->valueAsBool("ignoresslerrors")));
}

void QmlApplicationViewer::saveAccount(QVariant uname,
                                       QVariant pword,
                                       QVariant server,
                                       QVariant usessl,
                                       QVariant ignoresslerrors,
                                       QVariant aenabled)
{

    connect(accountx,
            SIGNAL(synced()),
            SLOT(onAccountSaved()));

    accountx->setValue("server", server);
    accountx->setValue("username", uname);
    accountx->setValue("password", pword);
    accountx->setValue("usessl", usessl);
    accountx->setValue("ignoresslerrors", ignoresslerrors);
    accountx->setEnabled(aenabled.toBool());
    accountx->setDisplayName(server.toString());
    accountx->sync();
}

void QmlApplicationViewer::onAccountSaved()
{
    Accounts::AccountId id = accountx->id();
    de::buschmann23::ocNewsEngine::Configuration* configIf = new de::buschmann23::ocNewsEngine::Configuration("de.buschmann23.ocNewsEngine", "/Configuration", QDBusConnection::sessionBus(), this);
    QDBusVariant dbvValue(QVariant::fromValue(id));
    configIf->setSetting("account/id", dbvValue);
    exit(0);
}

void QmlApplicationViewer::deleteAccount()
{
    if (accountx != NULL)
    {
        connect(accountx,
                SIGNAL(removed()),
                SLOT(onAccountRemoved()));

        accountx->remove();
        accountx->sync();
    }
    else
        qWarning() << "deleteAccount error: accountx is NULL";
}

void QmlApplicationViewer::onAccountRemoved()
{
    de::buschmann23::ocNewsEngine::Configuration* configIf = new de::buschmann23::ocNewsEngine::Configuration("de.buschmann23.ocNewsEngine", "/Configuration", QDBusConnection::sessionBus(), this);
    QDBusVariant dbvValue(QVariant(""));
    configIf->setSetting("account/id", dbvValue);

    exit(0);
}

void QmlApplicationViewer::createAccount(QVariant uname,
                                         QVariant pword,
                                         QVariant server,
                                         QVariant usessl,
                                         QVariant ignoresslerrors)
{

    connect(manager,
            SIGNAL(accountCreated(Accounts::AccountId)),
            SLOT(onAccountCreated(Accounts::AccountId)));

    accountx = manager->createAccount("ocnews");
    accountx->setValue("username", uname);
    accountx->setValue("password", pword);
    accountx->setValue("server", server);
    accountx->setValue("usessl", usessl);
    accountx->setValue("ignoresslerrors", ignoresslerrors);
    accountx->setEnabled(true);
    accountx->setDisplayName(server.toString());
    accountx->sync();
}


void QmlApplicationViewer::onAccountCreated(Accounts::AccountId id)
{

    de::buschmann23::ocNewsEngine::Configuration* configIf = new de::buschmann23::ocNewsEngine::Configuration("de.buschmann23.ocNewsEngine", "/Configuration", QDBusConnection::sessionBus(), this);
    QDBusVariant dbvValue(QVariant::fromValue(id));
    configIf->setSetting("account/id", dbvValue);
    exit(0);
}
